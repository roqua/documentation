machine:
  ruby:
    version: 2.1.0
test:
  override:
    - bundle exec rake test
    - bundle exec rake validate:links:internal
deployment:
  production:
    branch: master
    commands:
      - bundle exec rake publish